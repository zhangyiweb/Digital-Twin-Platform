/** 导出项目包：相机默认位置（俯视原点附近） */
export const EXPORT_PACKAGE_DEFAULT_CAMERA_POSITION = { x: 15, y: 10, z: 15 };

/** 导出项目包：轨道控制点默认在原点 */
export const EXPORT_PACKAGE_DEFAULT_CONTROLS_TARGET = { x: 0, y: 0, z: 0 };
